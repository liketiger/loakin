import React from 'react'
import styled from 'styled-components';

const RaidInfo = () => {
  return (
    <RaidInfoWrapper>
      <RaidText>발탄</RaidText>
      <RaidText>하드</RaidText>
      <RaidText>pm6시</RaidText>
    </RaidInfoWrapper>
  )
};

const RaidInfoWrapper = styled.div`
  
`;

const RaidText = styled.span`

`;

export default RaidInfo;
